
- name: install common software
  yum:
    name: "{{item}}"
  with_items:
  - rsync

- name: add authorized key
  authorized_key:
    user: root
    state: present
    key: "{{ lookup('file', '~/.ssh/k8s_rsa.pub') }}"

